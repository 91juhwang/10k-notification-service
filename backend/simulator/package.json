{
  "name": "@microgrid/simulator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('simulator tests pending')\""
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
