{
  "name": "@microgrid/simulator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx src/index.ts",
    "load": "dotenv -e ../../.env -- tsx src/load-test.ts",
    "load:100k": "dotenv -e ../../.env -- env SIM_LOAD_USE_BATCH=true SIM_LOAD_BATCH_SIZE=100 SIM_LOAD_RATE_PER_MINUTE=100000 SIM_LOAD_DURATION_SECONDS=300 SIM_LOAD_MAX_IN_FLIGHT=600 SIM_LOAD_DEVICE_COUNT=2000 SIM_LOAD_TIMEOUT_MS=30000 tsx src/load-test.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('simulator tests pending')\""
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
